#!r49

$ENV['DEBUG'] && $DEBUG = 1
$METHOD = $ENV['METHOD']
$QUERY_PARAMS = {}

for param in ($ENV['QUERY_PARAMS'] || '').split(':')
	param == '' && break
	eval(sprintf("$%s = $QUERY_PARAMS[param] = getenv(param)", param))
end
